# Environment Variables

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRY=1h

# PostgreSQL Database Configuration (using Prisma)
DATABASE_URL="postgresql://user:password@localhost:5432/luxestay_db"

# Note: 
# 1. Install PostgreSQL locally or use a cloud service like Supabase, Railway, or Render
# 2. Update DATABASE_URL with your actual PostgreSQL connection string
# 3. Run: npm run prisma:migrate (to create database tables)
# 4. Never commit the actual .env file to version control!
# 5. Copy this file to .env and update the values

